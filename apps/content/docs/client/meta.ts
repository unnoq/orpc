import { defineMeta } from 'blume'

export default defineMeta({
  title: 'Client',
  pages: [
    'server-side',
    'client-side',
    'error-handling',
    'async-iterator-object',
    'dynamic-link',
  ],
})
